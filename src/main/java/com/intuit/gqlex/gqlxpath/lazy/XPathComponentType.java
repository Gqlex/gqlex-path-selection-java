package com.intuit.gqlex.gqlxpath.lazy;

/**
 * Enum representing different types of xpath components
 */
public enum XPathComponentType {
    FIELD,
    FRAGMENT,
    ARGUMENT,
    DIRECTIVE,
    OPERATION,
    ALIAS,
    VARIABLE
} 